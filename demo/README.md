# oci-registry-as-model-registry demo

## Running

Requirements:

- Go is in the `$PATH`
- [`reg`](https://github.com/genuinetools/reg) is in the`$PATH`
- The `oci-registry-as-model-registry` binary is in the `$PATH`
